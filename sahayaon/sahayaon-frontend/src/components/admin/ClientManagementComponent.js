import React from 'react';
import ClientGridManagement from './ClientGridManagement';

const ClientManagementComponent = ({ user }) => {
  return <ClientGridManagement user={user} />;
};

export default ClientManagementComponent;